# Ripple 整体任务清单

> 最后更新：Phase 9 评审前  
> 当前分支：main · HEAD = feat: M4-A P8 Yjs 生产级 CRDT 持久化

---

## 里程碑完成情况

| 阶段 | 状态 | 关键交付 |
|------|------|---------|
| **M1** Python 原型 | ✅ 完成并归档 | 9/9 单测，作为验证性原型 |
| **M2** Go 基础架构 | ✅ 完成 | PG/Neo4j/Redis/JWT/中间件 |
| **M3** 核心功能 | ✅ 完成 | SSE 编织 / Crystallize / 推荐器 / Spaces / 性能基线（22k QPS） |
| **M4-A Spike** Yjs 桥 | ✅ 完成（P5-T2） | 独立 :7790 广播 hub |
| **M4-A 生产化** CRDT 持久化 | ✅ 完成（Phase 8） | node_doc_states + REST + bridge 快照 + 前端写回 |
| **M4-B** 附件存储 | ✅ 本地 FS 完成 | multipart 上传 + SHA 去重 + 鉴权下载（MinIO **已排除**，见自学习日志） |
| **M4-C** PWA | ✅ 部分 | manifest + service worker（离线读节点）|
| **Phase 5**（T1-T7） | ✅ 完成 | 附件 UI / Yjs Spike / 推荐融合 / 图片占位 / E2E 脚手架 / 全链路压测 |
| **Phase 6**（P6-A~E） | ✅ 完成 | perma_post 修复 / Yjs 鉴权 / 协作 demo / WS 压测 / Playwright |
| **Phase 7**（P7-A~F） | ✅ 完成 | fail-closed / ws-only token / 并发注释 / 集成测试 / E2E / apiBase env |
| **Phase 8**（P8-A~F） | ✅ 完成 | node_doc_states PG / REST doc_state / yjs-bridge 快照加载 / CollabDemo nodeId |

---

## 当前待办（Phase 9 候选）

### P9-A：Redis Pub/Sub yjs-bridge 多实例扩容（TD-02 解决）

| 内容 | 说明 |
|------|------|
| yjs-bridge 广播改为 Redis Pub/Sub | 多实例 WS hub 通过 Redis channel 同步更新 |
| 解决 TD-02 | hub.broadcast 持读锁问题 → 单实例内依旧持锁但延迟已最小化 |
| `YJS_BRIDGE_REDIS_URL` 新增环境变量 | 非空时启用 Redis 广播模式 |

### P9-B：对象存储（TD-03 状态变更）

> **MinIO 已从路线图移除**（老板决定，见自学习日志）。  
> 本地 FS 附件存储保持现状。若未来需要分布式对象存储，候选方案：AWS S3 SDK / Cloudflare R2。  
> TD-03 **关闭**。

### P9-C：Three.js 节点图可视化（M5 预研）

| 内容 | 说明 |
|------|------|
| WebGL 力导向布局 | 节点作为粒子，边作为弹簧约束 |
| 已有文档参考 | `docs/system-design/系统开发文档-流体动效引擎实现 (WebGL).md` |

### 安全/架构技术债

| ID | 描述 | 优先级 | 状态 |
|----|------|--------|------|
| TD-02 | yjs-bridge hub.broadcast 持读锁（P6 审查 §5.2 延期） | P2 | 👉 P9-A 解决 |
| TD-03 | MinIO 未上线，M4-B 附件仍用本地 FS | P1 | ✅ **关闭**（MinIO 排除，本地 FS 保持） |
| TD-04 | WS 1000 并发跨机测试（当前只 300/同机） | P3 | 待排 |

---

## 进度估算

已完成：**M1~M3 全部 + M4-A（Spike + 生产化） + M4-B（本地 FS）+ M4-C（基础 PWA）+ Phase 5~8 全部**。  
M4 完整交付剩余：MinIO + Vision/Audio（M4-B 扩展），估计 **4~6 周**。  
M5（Three.js 可视化）+ M6（移动端）：**~8~16 周**。


---

## 里程碑完成情况

| 阶段 | 状态 | 关键交付 |
|------|------|---------|
| **M1** Python 原型 | ✅ 完成并归档 | 9/9 单测，作为验证性原型 |
| **M2** Go 基础架构 | ✅ 完成 | PG/Neo4j/Redis/JWT/中间件 |
| **M3** 核心功能 | ✅ 完成 | SSE 编织 / Crystallize / 推荐器 / Spaces / 性能基线（22k QPS） |
| **M4-A Spike** Yjs 桥 | ✅ 完成（P5-T2） | 独立 :7790 广播 hub |
| **M4-B** 附件存储 | ✅ 完成 | 本地 FS multipart 上传 + SHA 去重 + 鉴权下载 |
| **M4-C** PWA | ✅ 部分 | manifest + service worker（离线读节点）|
| **Phase 5**（T1-T7） | ✅ 完成 | 附件 UI / Yjs Spike / 推荐融合 / 图片占位 / E2E 脚手架 / 全链路压测 |
| **Phase 6**（P6-A~E） | ✅ 完成 | perma_post 修复 / Yjs 鉴权 / 协作 demo / WS 压测 / Playwright |
| **Phase 7**（P7-A~C） | ✅ 完成 | fail-closed / ws-only token / 并发注释 |

---

## 当前待办（已知）

### 立即可执行（来自本轮用户选择）

| ID | 任务 | 说明 |
|----|------|------|
| **P7-D** | ws_token 接口集成测试 | `TestWsToken_Issue`：正常换 token / ws token 续期被拒 / 无 token 401 |
| **P7-E** | Playwright e2e 更新 | 馆入湖后验证 CollabDemo 正确渲染（不需要跑 yjs-bridge，只验 DOM） |
| **P7-F** | 前端 apiBase 环境变量化 | `VITE_API_BASE` + `.env.local` 示例，溈除 `http://localhost:8080` 硬编码 |

### Phase 8 需求评审（十角色全流程）

> 待 Phase 8 需求评审完成后，由接口设计师确定具体任务 ID。  
> 候选方向（来自 M4 需求清单草案）：

| 方向 | 内容 | 状态 |
|------|------|------|
| M4-A 生产化 | Yjs CRDT 全集成（节点 doc 字段 + 持久化 + 离线合并） | ❌ 未开始 |
| M4-B MinIO | 对象存储替换本地 FS（附件 / 图片） | ❌ 未开始 |
| M4-B Vision | 图片上传 + AI 编织识别（vision provider） | ❌ 未开始 |
| M4-B Audio | 录音 + 自动转写（Whisper / 讯飞） | ❌ 未开始 |
| M4-C 移动端增强 | PWA 推送通知 / React Native（待决策） | ❌ 未开始 |

### 安全/架构技术债

| ID | 描述 | 优先级 |
|----|------|--------|
| TD-01 | CollabDemo apiBase 硬编码（→ P7-F） | P1 |
| TD-02 | yjs-bridge hub.broadcast 持读锁（P6 审查 §5.2 延期） | P2 |
| TD-03 | MinIO 未上线，M4-B 附件仍用本地 FS | P1 |
| TD-04 | WS 1000 并发跨机测试（当前只 300/同机） | P3 |

---

## 进度估算

目前完成约 **M3 全部 + M4 Spike + M4-B 本地存储 + M4-C 基础 PWA**，  
M4 完整交付（CRDT 生产化 + MinIO + Vision + Audio）还有 **~8–12 周** 工作量（按 M4 草案时间线）。  

下一个里程碑建议：**Phase 8 = M4-A 生产化 CRDT**（最高优先级业务价值 + 技术架构必选项）。
