# Ripple 整体任务清单

> 最后更新：2026-04-27
> 当前分支：main  
> 文档定位：项目里程碑 + 当前阶段任务 + 下一阶段规划（单一权威来源）

---

## 1. 里程碑总览

| 阶段 | 范围 | 状态 |
|------|------|------|
| M1 | Python 原型验证（FastAPI，9/9 单测） | ✅ 完成并归档 |
| M2 | Go 基础架构（PG / Neo4j / Redis / JWT / 中间件） | ✅ 完成 |
| M3 | 核心功能（SSE 编织 / Crystallize / 推荐器 / Spaces / 22k QPS 基线） | ✅ 完成 |
| M4-A | Yjs CRDT 协作（Spike → 持久化 → 桥快照 → 多实例 Pub/Sub） | ✅ 完成 |
| M4-B | 附件存储（本地 FS + SHA 去重 + 鉴权下载，MinIO 已排除） | ✅ 完成 |
| M4-C | PWA 基础（manifest + service worker 离线读节点） | ✅ 完成 |
| M5-A | Three.js 完整动效（粒子流 + 弹力边 + 编织弹入） | ✅ 完成 |
| Phase 5 ~ 11 | 详见下方分阶段清单 | ✅ 全部完成 |
| **Phase 12** | 批量导入 / 全文检索 / 多租户 / 集成测试门槛 / 技术债清零 | ✅ **本期完成** |
| **Phase 13** | **启动测试与发布准备**（联调环境、性能基线复测、灰度准入） | 🚧 **已启动** |

---

## 2. Phase 5 ~ 11 交付一览

| 阶段 | 关键交付 |
|------|---------|
| Phase 5（T1-T7） | 附件 UI / Yjs Spike / 推荐融合 / 图片占位 / E2E 脚手架 / 全链路压测 |
| Phase 6（A-E） | perma_post 修复 / Yjs 鉴权 / 协作 demo / WS 压测 / Playwright |
| Phase 7（A-F） | fail-closed / ws-only token / 并发注释 / 集成测试 / E2E / apiBase env |
| Phase 8（A-F） | node_doc_states PG / REST doc_state / yjs-bridge 快照加载 / CollabDemo nodeId |
| Phase 9（A/C/D） | Redis Pub/Sub yjs-bridge 多实例 / Three.js 力导向图谱 / 跨机压测方案 |
| Phase 10（A/B/C） | API Key 鉴权 / 审计日志 / 湖成员角色变更 |
| Phase 11（A-E + M5-A） | API Key UI / 审计日志 UI / 成员管理 UI / Prometheus 指标 / Docker Compose / Three.js 完整动效 |

---

## 3. Phase 12 已交付（本期）

### 3.1 功能项

| 编号 | 任务 | 后端 | 前端 | 关键产物 |
|------|------|------|------|---------|
| P12-A | 节点批量导入（CSV / JSON） | `POST /api/v1/lakes/{id}/nodes/batch`（上限 1000 行） | `ImportModal.tsx` + Home 页 📥 入口 | 空/纯空白行跳过；> 10000 字符自动截断；服务层 3 个边界测试全 PASS |
| P12-C | 多租户 / 组织（Org Scoping） | `migrations/0013_organizations.up.sql` + `repo_org.go` + `service/org.go` | `OrgPanel.tsx`（含「按邮箱邀请」表单）+ `api.addOrgMemberByEmail` | `/api/v1/organizations` CRUD + `/members` + `/members/by_email` + `PATCH /lakes/{id}/org` |
| P12-D | 全文检索 | `GET /api/v1/search?q=&lake_id=&limit=` 走 Neo4j fulltext `node_content_fts` | `SearchModal.tsx` + `Cmd/Ctrl+K` 快捷键 | 节点主存于 Neo4j，避免双写一致性，故未走 PG `tsvector` |

### 3.2 工程项

| 任务 | 状态 | 说明 |
|------|------|------|
| 集成测试门槛清理 | ✅ | `RIPPLE_INTEGRATION=1` 自动加载 `backend-go/.env`；最小集成链路不再 skip |
| 文档命名收敛 | ✅ | `docs/system-design` 去 D/G/M 前缀，`docs/README.md` 导航改为语义化名称 |
| 构建复验 | ✅ | `go build ./...`、`go test ./internal/api/http -short`、`npm run build` 均通过 |

### 3.3 技术债清零

| ID | 描述 | 结果 |
|----|------|------|
| TD-02 | yjs-bridge hub.broadcast 持读锁 | ✅ 关闭（P9-A Redis Pub/Sub 已缓解） |
| TD-03 | MinIO 未上线 | ✅ 关闭（架构决议保留本地 FS） |
| TD-05 | API Key 返回字段 `key` 与前端契约 `raw_key` 不一致 | ✅ 关闭（同时返回 `raw_key` 与 `key`，集成测 `TestIntegration_APIKeyFlow` 通过） |
| TD-06 | 批量导入上限 100 行与验收标准 1000 行不一致 | ✅ 关闭（`maxBatchImport` 升档至 1000，3 个边界测试 PASS） |
| TD-04 | 1000 WS 连接跨机压测 | ✅ 首轮远端压测已执行（`docs/dev/TD-04-WS-loadtest-report-20260427-112224.md`）：1000 建连成功、0 失败、握手 p95 183.743ms；补采样（`docs/dev/TD-04-WS-loadtest-report-20260427-135833.md` + `docs/dev/td04-sample-20260427-135833.log`）与后续复跑（`docs/dev/TD-04-WS-loadtest-report-20260427-140045.md`）暴露出建湖后立即压测会踩到 lake 投影视图延迟。远端 Linux 诊断（`docs/dev/TD-04-WS-loadtest-report-20260427-142214.md`）完成根因收口后，clean rerun（`docs/dev/TD-04-WS-loadtest-report-20260427-142125.md`）已恢复到 `1000 / 1000` 成功、`p95 175.797ms`、`p99 192.333ms`，TD-04 正式关闭 |

### 3.4 候选但未启动

| 任务 | 状态 | 决策 |
|------|------|------|
| P12-B GraphQL API 层 | ⏸ 暂缓 | 当前 REST 性能与聚合需求未触发；不进入 Phase 13 |
| P12-E PWA 完善（离线写 + Web Push） | ⏸ 部分 | 已有离线读节点；离线写队列与 Web Push 留待 Phase 13 后期评估 |

---

## 4. Phase 13 启动测试与发布准备（进行中）

> 计划窗口：2026-04-29 ~ 2026-05-23（约 4 周）  
> 阶段目标：从「功能完整」过渡到「可发布」——具备一份可复现的联调环境、可信的性能基线、明确的灰度准入清单。

**当前进展（2026-04-27）**：Sprint 13.1 已完成标准 bootstrap + smoke 一键验收；远端 Docker 主机 `fn.cky` 上的 staging 中间件、`backend`、`yjs-bridge`、frontend 均已按标准链路拉起，`/healthz` 返回 `{"status":"ok"}`，`scripts/smoke/phase13-smoke.ps1 -Base http://fn.cky:18000` 已全绿。

**当前阻塞（2026-04-27）**：标准 `migrate` 容器空库探测已修复，frontend Docker build 也已在远端通过；TD-04 现已用远端 Linux clean rerun 收口。Redis / Neo4j / yjs-bridge 故障演练、10k 搜索基线与 1000 行批量导入 5s 门槛均已完成。`teardown-staging.ps1 -KeepVolumes -DryRun` 非破坏回收演练也已通过。当前剩余工作为：回滚验收、staging 实际回收窗口、手册完整拉起+实际回收复现。

### 4.1 阶段定位

Phase 12 已交付批量导入、全文检索、多租户、API Key、审计日志、成员管理等所有核心闭环功能；后端、前端、Yjs 桥、Prometheus 指标、Docker Compose 全栈编排均稳定。Phase 13 不再增加新功能模块，重心转向**可信交付**：

- 在与生产同构的联调环境中跑通所有关键路径；
- 把 1000 WS 跨机压测、批量导入端到端、Org 邀请端到端等场景从「单元测试通过」推进到「环境验收通过」；
- 输出灰度准入清单、回滚预案与运维手册。

### 4.2 Sprint 13.1（第 1 周）联调环境与冒烟

| 优先级 | 任务 | 可交付物 | 验收标准 |
|--------|------|----------|---------|
| P0 | 联调环境固化 | `docker-compose.staging.yml` + 一键 bootstrap 脚本（迁移 + 健康检查） | 任意干净机器 30 分钟内拉起完整栈，`/healthz` 全绿 |
| P0 | 冒烟用例集 | `scripts/smoke/phase13-smoke.ps1`：注册 → 登录 → 建湖 → 全文检索 → 批量导入 → API Key → Org 邀请 → 审计日志 | 一键全绿，失败用例输出 trace 与 commit |
| P1 | CI 集成测试矩阵 | GitHub Actions：单元 + `RIPPLE_INTEGRATION=1` 集成 + 前端构建 | 主分支 push 后 < 10 分钟出绿/红结论 |

**增量落地（2026-04-27）**：`.github/workflows/ci.yml` 已新增 Phase 13 integration job，使用 `docker-compose.staging.yml` 拉起 PG / Neo4j / Redis，执行 migrate 与 `TestIntegration` 测试；同时新增 `scripts/teardown-staging.ps1` 作为回滚/回收入口。

**文档与演练入口（2026-04-27）**：已新增 `docs/launch/Phase13-准入清单.md`、`docs/ops/Phase13-联调与回滚手册.md` 与 `scripts/drill-staging.ps1`；`docs/快速上手.md` 已补 Phase 13 推荐启动方式。

**进一步收敛（2026-04-27）**：`.github/workflows/ci.yml` 已新增 `staging-smoke` job，直接调用 `bootstrap-staging.ps1` 与 `phase13-smoke.ps1`；`docs/README.md` 与 `docs/dev/开发者-Onboarding指南.md` 已切换到 Go / Phase 13 现状；新增 `docs/dev/Phase13-故障演练验收记录模板.md` 作为演练回填入口。

**回滚演练能力（2026-04-27）**：已新增 `scripts/rollback-staging.ps1`，支持在干净工作树下按指定 git ref 执行 staging 级回滚演练；CI 中间件健康检查也已改为基于 `docker inspect` 的确定性判定。

**真实联调准备（2026-04-27）**：已修复 `phase13-smoke.ps1` 中 Org 邀请重复成员的假失败路径；新增 `docs/dev/Phase13-真实联调执行清单.md` 作为第一次 Docker 联调与结果回填入口；`docs/dev` 旧前端检查与周报文档已标注为历史归档。

**执行回填入口（2026-04-27）**：已新增 `docs/dev/Phase13-联调结果回填模板.md`；`bootstrap/teardown/drill/rollback` 脚本均补上 Docker 命令存在性检查；CI 失败时会自动输出中间件或 staging 服务日志，便于定位问题。

**文档补漏（2026-04-27）**：已新增 `docs/dev/Phase13-联调结果整理话术.md`，`backend-go/README.md` 也已从历史占位态切换为当前 Go 主线与 Phase 13 联调入口说明。

**真实联调回填（2026-04-27）**：

- 远端主机：`fn.cky`（Docker 28.5.2 / Compose v2.40.3）；
- staging 端口：backend `18000`、yjs-bridge `17790`、Postgres `25432`、Redis `26379`、Neo4j HTTP `27474`、Neo4j Bolt `27687`；
- 已验证链路：注册、登录、建湖、建节点、全文检索、批量导入、API Key、组织创建、按邮箱邀请、审计日志查询；
- 验证结论：当前 Go 主线已具备 Phase 13 标准 bootstrap 与远端联调能力，`migrate` 容器与 frontend Docker build 均已打通；TD-04 的重复压测退化也已确认不是 Linux 客户端或 WS 路由缺陷，而是建湖后未等待 lake 投影视图可读。

### 4.3 Sprint 13.2（第 2 周）性能与稳定性

| 优先级 | 任务 | 可交付物 | 验收标准 |
|--------|------|----------|---------|
| P0 | TD-04 1000 WS 跨机压测 | 报告 `docs/dev/TD-04-WS压测报告-<ts>.md`（CPU/内存曲线 + p95 延迟 + 失败率） | 1000 并发持续 30s，连接成功率 ≥ 99%，p95 < 200ms |
| P1 | 全文检索性能基线 | 10k 节点造数 + p95 实测，补入 `M3-性能基线对照.md` | p95 < 300ms |
| P1 | 批量导入端到端验收 | 上传 1000 行 CSV → 全部入库 + 坏行报告 | 成功率 100%，UI 5s 内回响应 |
| P2 | 故障注入演练 | 杀 yjs-bridge / Redis / Neo4j 各 1 次，记录恢复时间 | 业务自动恢复，数据无丢失 |

**性能回填更新（2026-04-27）**：`docs/dev/TD-04-WS-loadtest-report-20260427-112224.md` 与远端 Linux clean rerun `docs/dev/TD-04-WS-loadtest-report-20260427-142125.md` 均已满足 Phase 13 WS 门槛。补采样 `docs/dev/TD-04-WS-loadtest-report-20260427-135833.md` 与 `docs/dev/td04-sample-20260427-135833.log` 暴露的 `739/1000`、`516/1000` 退化，已由诊断报告 `docs/dev/TD-04-WS-loadtest-report-20260427-142214.md` 收口为测试流程问题：建湖后未等待 lake 投影视图可读。当前 WS 压测项已关闭。

**检索基线回填更新（2026-04-27）**：远端 `fn.cky` 已完成全文检索 `10k` 节点环境验收。通过 `10` 批次 `x 1000` 节点导入专用 lake、轮询最后一批 marker 可检索后，对 `/api/v1/search` 连续采样 `50` 次，结果为 `p50 41.2039ms`、`p95 66.4085ms`、`p99 117.9482ms`，满足 `p95 < 300ms` 门槛。详见 `docs/dev/Phase13-全文检索10k基线-20260427.md`。

**稳定性回填更新（2026-04-27）**：远端 `fn.cky` 已完成 Redis / Neo4j / yjs-bridge 故障演练。前两项均在 stop/start 后恢复到健康态并通过 smoke；yjs-bridge 首次演练暴露恢复链路缺陷，已通过切换到 `golang:1.23-alpine`、修正 `RIPPLE_JWT_SECRET` 与 `YJS_BRIDGE_REDIS_URL` 配置收口，修复后 `http://fn.cky:17790/healthz` 返回 `200 ok`，日志显示 `redis pub/sub enabled`。详见 `docs/dev/Phase13-故障演练验收记录-20260427.md`。

### 4.4 Sprint 13.3（第 3 周）发布准备

| 优先级 | 任务 | 可交付物 | 验收标准 |
|--------|------|----------|---------|
| P0 | 灰度准入清单 | `docs/launch/Phase13-准入清单.md`（功能 / 性能 / 安全 / 运维 5 大维度） | 决策者签字 |
| P0 | 回滚预案 | 数据库迁移 down 演练 + 容器镜像回退脚本 | 演练通过且 < 5 分钟回滚 |
| P1 | 运维手册 V1 | `docs/ops/`：日志、指标、告警阈值、常见故障 SOP | 由非作者按手册独立完成一次故障处置 |
| P2 | 端用户文档 V1 | `docs/快速上手.md` 增补全文检索、批量导入、Org 邀请章节 | 体验方按手册完成全流程 |

### 4.5 Sprint 13.4（第 4 周）灰度与收尾

| 优先级 | 任务 | 可交付物 | 验收标准 |
|--------|------|----------|---------|
| P0 | 灰度发布（内测名单 ≤ 20 人） | 发布 tag `v0.13.0` + 灰度入口 | 7 日 P0 缺陷数 = 0 |
| P1 | 体验方反馈收敛 | 反馈表 + 修复清单 | 全部 P1 反馈在 Phase 14 入口前关闭或排期 |
| P2 | Phase 14 候选评估 | P12-B GraphQL / P12-E PWA 离线写 / Org 配额计费 任选其一立项 | 决策者裁决 |

### 4.6 启动前置（Phase 13 开工前必须完成）

1. CI 注入 `RIPPLE_INTEGRATION=1`，并确保可读取 `backend-go/.env` 或等价密钥源；
2. 联调环境的中间件版本与生产对齐（PG 16、Neo4j 5、Redis 7）；
3. 1000 WS 压测机分配（独立网段，避开开发机）。

---

## 5. 中间件与环境现状

| 目标 | 状态 | 说明 |
|------|------|------|
| 本地 127.0.0.1:5432/6379/7687 | ❌ 不可达 | 当前开发机未启本地容器；不阻塞集成测试 |
| 远程 fn.cky:15432/16379/7687 | ✅ 可达 | 用于联调与跨机压测 |
| Go 集成测试（最小子集） | ✅ PASS | `RIPPLE_INTEGRATION=1` 自动加载 `.env` |
| Yjs 桥 :7790 | ✅ 多实例就绪 | Redis Pub/Sub 广播 |
| Prometheus `/metrics` | ✅ 就绪 | DB pool gauges + HTTP histogram |

---

## 6. 架构技术选型当前状态

| 层 | 选型 | 版本 | 状态 |
|----|------|------|------|
| 后端 | Go + chi/v5 + pgx/v5 + neo4j-driver/v5 | Go 1.23.4 | 稳定 |
| 前端 | React 18 + Vite + TypeScript + Three.js | React 18.x | 稳定 |
| PG | PostgreSQL | 16+ | 稳定，已 16 次迁移 |
| 图 | Neo4j | 5+ | 稳定（fulltext index 已用于 P12-D） |
| 缓存 / 广播 | Redis | 7+ | 稳定 |
| 对象存储 | 本地 FS | — | 架构决议保留 |
| Yjs 桥 | Go ws :7790 + Redis Pub/Sub | Go 1.23+ | 多实例就绪 |
| 部署 | Docker Compose | Compose v2 | Phase 13 将补 staging 编排 |

---

## 7. 进度小结

- **已完成**：M1 ~ M11 全部 + Phase 12 全部功能项与技术债清零（TD-04 留待联调环境执行）。
- **进行中**：Phase 13 已完成标准远端 bootstrap、smoke、Linux 客户端 TD-04 收口、Redis/Neo4j/yjs-bridge 故障演练、`10k` 全文检索基线与 `1000` 行批量导入 5s 门槛；当前剩余回滚验收与运维复现项。
- **暂缓**：P12-B GraphQL、P12-E PWA 离线写 / Web Push、Org 配额计费，列为 Phase 14 候选。

### 启动前置清单（必须完成）

1. 在 CI 注入 `RIPPLE_INTEGRATION=1`，并确保可读取 `backend-go/.env` 或等价密钥源。
2. 固化一键命令：`go test ./internal/api/http/... -run "TestIntegrationHealthz|TestIntegrationAuthGuard" -count=1 -v`。
3. 输出第一版 Phase 12 每周燃尽看板（任务 owner + 预计完成日）。

---

## Phase 11 完成情况

| ID | 任务 | 状态 | Commit |
|----|------|------|--------|
| **M5-A** | Three.js 完整动效（粒子流 + 弹力边 + 编织弹入） | ✅ | fe9879d |
| **P11-A** | API Key 管理 UI | ✅ | （Phase 10 同批） |
| **P11-B** | 审计日志浏览 UI | ✅ | （Phase 10 同批） |
| **P11-C** | 湖成员角色管理 UI（`GET /lakes/{id}/members` + LakeMemberManager 组件） | ✅ | 56ca422 |
| **P11-D** | Prometheus metrics（DB pool acquired/total gauges） | ✅ | 42ad65f |
| **P11-E** | Docker Compose 全栈一键部署 | ✅ | 0cd7021 |

---

## Phase 12 候选任务

> 优先级评估基准：用户价值 × 技术可行性 ÷ 工作量
> 接受本阶段任务前需经十角色团队流程评审。
> **2026-04-26 真实进度对账**：本轮代码侧审计发现 P12-A / P12-C / P12-D 已在主线交付，仅文档滞后；P12-B / P12-E 仍为候选。

### P12-A：批量节点导入（CSV / JSON） · ✅ 已落地（含 1000 行验收）

| 属性 | 说明 |
|------|------|
| 目标 | 允许用户通过上传 CSV/JSON 文件一次性创建多个节点（含标签、内容、边关系） |
| 后端 | ✅ `POST /api/v1/lakes/{id}/nodes/batch`（`handlers_node.go::BatchImport` + `service/node.go::BatchImportNodes`，`maxBatchImport=1000`，超出返回 `ErrInvalidInput`） |
| 前端 | ✅ `frontend/src/components/ImportModal.tsx` + Home 页 📥 按钮 |
| 测试 | ✅ `internal/service/node_batch_import_test.go`：`TestNode_BatchImport_1000Rows` / `_BadRowsSkipped` / `_OverLimit` 全部 PASS |
| 验收 | 1000 行成功率 100%；空/纯空白行跳过；超过 10000 字符自动截断；> 1000 行返回 400 |

### P12-B：GraphQL API 层 · ⏸ 暂缓

| 属性 | 说明 |
|------|------|
| 目标 | 在现有 REST 之上提供 GraphQL 端点，减少前端多次 fetch |
| 技术 | `github.com/99designs/gqlgen`（Go codegen） |
| 范围 | 只读查询（lakes / nodes / edges / members）+ 订阅（节点变更推送） |
| 估算 | XL（约 4 周） |
| 决策 | 当前 REST 性能足够，前端聚合需求不强；优先完成 P12-E 与 TD-04 |

### P12-C：多租户 / 组织（Organization Scoping） · ✅ 已落地（含 by_email 邀请）

| 属性 | 说明 |
|------|------|
| 后端 | ✅ `migrations/0013_organizations.up.sql` + `internal/store/repo_org.go` + `internal/service/org.go` |
| 路由 | ✅ `/api/v1/organizations`（CRUD + 成员管理）+ `PATCH /api/v1/lakes/{id}/org` 绑定湖归属 |
| 邀请 | ✅ `POST /api/v1/organizations/{id}/members/by_email`（本轮新增）——免用户手工拼 user_id；未注册返回 404 |
| 前端 | ✅ `frontend/src/components/OrgPanel.tsx`（含“按邮箱邀请”表单）+ `api.addOrgMemberByEmail` |
| 缺口 | Org 配额 / 计费策略未定义（不在 P12 范围） |

### P12-D：全文检索 · ✅ 已落地（实现偏离原计划）

| 属性 | 说明 |
|------|------|
| 目标 | 节点内容全文检索，支持中英文分词 |
| 实际技术 | ⚠️ **Neo4j fulltext index `node_content_fts`**（`backend-go/scripts/neo4j_constraints.cypher` 声明），非原计划的 PG `tsvector` |
| 后端 | ✅ `GET /api/v1/search?q=...&lake_id=...&limit=...`（`handlers_node.go::Search` + `service/node.go::SearchNodes` + `repo_node.go::cypherSearchNodes`） |
| 前端 | ✅ `SearchModal.tsx` + Home 页 Cmd/Ctrl+K 快捷键 |
| 决策记录 | 选择 Neo4j 而非 PG tsvector 的原因：节点主存于 Neo4j，避免双写一致性；`zhparser`/`pg_jieba` 在生产部署需扩展安装 |
| 后续 | 在 10k 节点数据下采集 p95 数据并补入 `M3-性能基线对照.md`（候选） |

### P12-E：移动端 PWA 完善 · ⏸ 部分

| 属性 | 说明 |
|------|------|
| 目标 | 补全 M4-C：离线写节点合并（IndexedDB + Yjs） + 推送通知 |
| 已落地 | ✅ `frontend/index.html` 注册 `sw.js`（离线读节点） |
| 缺口 | ❌ Workbox 离线写队列；❌ Web Push（VAPID 密钥/订阅/后端推送） |
| 决策 | 移到 Phase 13 候选；当前优先级让位 TD-04 与 P12-A 验收闭环 |

---

## Phase 12 优先级建议

| 优先级 | 任务 | 状态 | 备注 |
|--------|------|------|------|
| P0 | TD-05 API Key 契约修复 | ✅ 已完成 | 本轮 commit |
| P1 | **P12-D** 全文检索 | ✅ 后端+前端均已落地 | 实现走 Neo4j fulltext，与原计划 PG tsvector 偏离已记录 |
| P2 | **P12-A** 批量导入 | ✅ 主链路已落地 | 待补 1000 行端到端用例 |
| P3 | **TD-04** 1000 WS 跨机压测 | ⚠️ 待执行 | 脚本就位，需指定执行窗口与机器 |
| P4 | **P12-C** 多租户 | ✅ 含 by_email 邀请 + UI 表单 | 配额/计费留 Phase 13 |
| P5 | **P12-E** PWA 完善 | ⏸ 暂缓至 Phase 13 | 离线写 + Web Push 范围较大 |
| P6 | **P12-B** GraphQL | ⏸ 暂缓 | REST 当前足够，等产品价值再启动 |

---

## 架构技术选型当前状态

| 层 | 选型 | 版本 | 状态 |
|----|------|------|------|
| 后端 | Go + chi/v5 + pgx/v5 + neo4j-driver/v5 | Go 1.23.4 | 稳定 |
| 前端 | React 18 + Vite + TypeScript + Three.js | React 18.x | 稳定 |
| PG | PostgreSQL | 16+ | 稳定，已 12 次迁移 |
| 图 | Neo4j | 5+ | 稳定 |
| 缓存 | Redis | 7+ | 稳定 |
| 对象存储 | 本地 FS | — | MinIO 已排除 |
| Yjs 桥 | Node.js ws :7790 + Redis Pub/Sub | — | 多实例就绪 |
| 部署 | Docker Compose | Compose v2 | P11-E 完成 |

---

## 进度估算

已完成：**M1~M11 全部**（Phase 5~11 + M4 全系列 + M5-A）。  
下一阶段：**Phase 12**（已进入启动执行，先做 P12-D + 中间件测试门槛清理）。
