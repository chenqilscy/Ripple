# Phase 13 回滚验收待办（已完成，2026-04-27）

## 结论

已完成，详见 `docs/dev/Phase13-rollback-acceptance-20260427.md`。

## 原阻塞与处理

1. 远端运行目录 `/home/admin/Ripple` 是部署副本，缺少 `.git`，无法直接做 git ref 级回滚；
2. 本地开发机缺少 Docker CLI，无法在本机直接执行 `scripts/rollback-staging.ps1` 完整链路；
3. 现网 staging 正在承载已验收结果，直接在同目录切 ref 风险过高。

## 处理结果

1. 远端 clone TLS 不稳定，改用本地 `git archive` + `scp`；
2. 已验证 `a9834d0` 回滚态 smoke 全绿；
3. 已验证 `c6648a3` 恢复态 smoke 全绿；
4. 已执行数据库 `down` / `up` 演练，演练后 smoke 全绿；
5. 已回填准入文档和整体任务清单。

## 验收口径（预留）

- 回滚后 smoke 全绿；
- 恢复后 smoke 全绿；
- 数据库 down/up 后 smoke 全绿；
- 全过程有可追溯命令、日志、时间戳。