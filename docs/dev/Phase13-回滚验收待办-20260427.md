# Phase 13 回滚验收待办（2026-04-27）

## 结论

按当前环境条件，回滚验收本轮暂缓执行，不强行验证。

## 当前阻塞

1. 远端运行目录 `/home/admin/Ripple` 是部署副本，缺少 `.git`，无法直接做 git ref 级回滚；
2. 本地开发机缺少 Docker CLI，无法在本机直接执行 `scripts/rollback-staging.ps1` 完整链路；
3. 现网 staging 正在承载已验收结果，直接在同目录切 ref 风险过高。

## 待办（下轮优先）

1. 准备独立回滚窗口：
   - 在远端创建带 `.git` 的干净 clone/worktree；
   - 使用隔离 compose project 名与隔离端口，避免影响当前 staging。
2. 在独立窗口执行回滚验收：
   - 切到目标稳定 ref；
   - 执行 teardown + bootstrap；
   - 运行 `scripts/smoke/phase13-smoke.ps1` 验证回滚后主链路。
3. 回填准入文档：
   - 更新 `docs/launch/Phase13-准入清单.md` 回滚项状态；
   - 同步 `docs/system-design/整体任务清单.md` 当前阻塞描述。

## 验收口径（预留）

- 回滚后 smoke 全绿；
- 回滚窗口恢复时长满足目标（`< 5 min`）；
- 全过程有可追溯命令、日志、时间戳。