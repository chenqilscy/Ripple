# PR 标题（建议）

feat: 扩展组织通知模板与分享能力并完成文档收敛

---

## 背景

本次提交将此前混合改动拆分为 4 个可审阅提交（docs/chore/fix/feat），目标是：

- 收敛 system-design 文档命名与链接引用，降低维护成本。
- 修复分享页与画布注释中的文案和编码噪音（BOM）。
- 完成前后端一批组织/通知/模板/分享相关功能扩展。

---

## 变更概览

### 提交 1
- 提交：`2e49453`
- 标题：`docs: 文档重命名收敛与引用修复`
- 统计：39 files changed, 247 insertions(+), 296 deletions(-)
- 重点：
  - `docs/system-design` 旧前缀文档（D/G/M）迁移为语义化命名。
  - 修复跨文档引用与导航入口。

### 提交 2
- 提交：`3c4addd`
- 标题：`chore: 配置与说明文档同步更新`
- 统计：5 files changed, 10 insertions(+), 6 deletions(-)
- 重点：
  - 根目录与工程说明同步。
  - 生成物/配置类文件同步（含 `frontend/tsconfig.tsbuildinfo`）。

### 提交 3
- 提交：`2da0a15`
- 标题：`fix: 分享链接与画布文案问题修复`
- 统计：5 files changed, 40 insertions(+), 9 deletions(-)
- 重点：
  - 分享链接展示改为前端统一生成公开 URL。
  - 修复画布注释文案误替换。
  - 去除 BOM 噪音，避免编码差异导致无意义 diff。

### 提交 4
- 提交：`83d7dae`
- 标题：`feat: 扩展组织通知模板与分享能力`
- 统计：24 files changed, 2215 insertions(+), 74 deletions(-)
- 重点：
  - 前端：新增组织、通知、搜索、导入、模板、分享、历史/diff 等组件与交互。
  - 前端：`Home.js` 与 API 客户端扩展多个能力端点。
  - 后端：部分 API/migration 配套变更。

---

## 关键文件

- 文档：
  - `docs/system-design/*`
  - `docs/README.md`
  - `docs/user-story/story.md`
- 前端：
  - `frontend/src/pages/Home.js`
  - `frontend/src/pages/Home.tsx`
  - `frontend/src/api/client.js`
  - `frontend/src/components/*`
  - `frontend/src/pages/SharedNode.js`
- 后端：
  - `backend/app/api/v1/*`
  - `backend/app/core/*`
  - `backend/app/ws/hub.py`
  - `backend-go/migrations/0004_lake_invites.up.sql`
  - `backend-go/migrations/0006_spaces.up.sql`

---

## 验证结果

- 工作区状态：clean（`git status` 无输出）。
- 文档旧命名链接：未检出旧前缀文件路径链接。
- BOM：目标文件已清理。

---

## 风险与回滚

- 风险：功能提交体量较大（第 4 提交），建议优先关注 `frontend/src/pages/Home.js` 与 `frontend/src/api/client.js` 的行为回归。
- 回滚：
  - 可按提交粒度回滚：`git revert 83d7dae` 等。
  - 如仅需撤销文档迁移，可单独 revert `2e49453`。

---

## 备注

如需改为“两提交模式（docs+code）”，见：`docs/dev/两提交压缩方案-2026-04-26.md`。
