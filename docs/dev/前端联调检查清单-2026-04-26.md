# 前端联调检查清单（2026-04-26）

目标：覆盖本轮高风险路径（分享、组织、通知、导入、模板、历史、搜索）。

---

## A. 分享链路

1. 打开分享页
- 访问 `/share/test-token`。
- 期望：页面可渲染，无登录态依赖崩溃。

2. 无效 token
- 访问不存在 token。
- 期望：显示失效提示，不出现白屏。

3. 节点分享创建与复制
- 在主页面为节点创建分享链接。
- 期望：展示公开 URL，复制按钮可用。

4. 分享撤销
- 撤销已有分享。
- 期望：UI 状态更新为已撤销，复制/访问行为受限。

---

## B. 组织与成员

1. 组织列表与详情
- 打开组织面板，切换组织。
- 期望：列表可加载，成员列表无报错。

2. 成员角色修改
- OWNER/ADMIN 对成员改角色。
- 期望：成功提示或列表即时更新；本人不可非法降级关键角色。

3. 成员移除
- 移除普通成员。
- 期望：确认弹窗后成功移除；失败有错误提示。

4. 组织与湖关联
- 查看组织下 lakes tab。
- 期望：空态和非空态都正确展示。

---

## C. 通知系统

1. 铃铛角标
- 触发后端通知事件或模拟新通知。
- 期望：角标增长，打开面板可见新增项。

2. 单条已读
- 点击单条未读通知。
- 期望：标记已读，角标递减。

3. 全部已读
- 点击“全部已读”。
- 期望：全部状态更新，角标归零。

4. 下拉分页
- 加载更多通知。
- 期望：分页追加正常，无重复/乱序。

---

## D. 导入与模板

1. 导入 JSON
- 使用合法 JSON 批量导入。
- 期望：解析成功、导入数量正确、节点可见。

2. 导入 CSV
- 使用带 header 的 CSV。
- 期望：字段映射正确，空行/异常行被安全处理。

3. 导入异常分支
- 提交格式错误内容。
- 期望：有明确错误提示，不会卡死。

4. 模板预览与新建
- 打开模板库、预览模板、新建模板。
- 期望：预览折叠/展开正常，新建后可立即使用。

---

## E. 历史与 Diff

1. 版本历史面板
- 打开节点历史。
- 期望：时间线可展示，空态正常。

2. Diff 对比
- 选择两个版本对比。
- 期望：增删标识正确、内容可读。

3. 回滚
- 回滚到历史版本。
- 期望：节点内容更新、失败时有错误提示。

---

## F. 搜索与图谱

1. 快捷键
- 使用 Ctrl/Cmd+K。
- 期望：搜索浮层开关正常。

2. 关键词搜索
- 输入关键词并选择结果。
- 期望：结果列表正确，点击可定位/关闭浮层。

3. 图谱高亮
- 在 graph 模式输入搜索词。
- 期望：节点高亮准确，不影响基础交互。

---

## G. 回归与兼容

1. 登录/退出
- 校验主流程无回归。

2. WS 在线状态
- 在线人数展示与连接指示正常。

3. 移动端视口
- 检查关键弹层与按钮可点击区域。

4. 控制台
- 关注报错与未处理 Promise。

---

## 验收建议

- 必测通过门槛：A/B/C/D/E 六类中每类至少通过 2 条。
- 上线门槛：A（分享）+ B（组织）+ C（通知）必须全通过。
