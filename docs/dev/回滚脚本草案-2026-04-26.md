# 回滚脚本草案（历史归档）

本文件已不再作为当前主入口，保留仅作历史参考。

Phase 13 当前回滚入口已切换为：

1. 运维手册：`docs/ops/Phase13-联调与回滚手册.md`
2. staging 回收：`scripts/teardown-staging.ps1`
3. staging 回滚演练：`scripts/rollback-staging.ps1 -Ref <stable-ref>`

若需要做主干提交级别的 `git revert`，仍建议沿用“先 staging 演练，再主干执行”的原则。
