# 回滚脚本草案（2026-04-26）

适用范围：main 上本次收口相关提交。

## 目标提交

- 合并提交：0912625
- 周报提交：76da1a3
- 修复提交：d8b16b1

## 方案 A：按提交粒度回滚（推荐）

```bash
# 1) 拉取最新主干
git checkout main
git pull --ff-only origin main

# 2) 回滚修复提交（不改历史）
git revert --no-edit d8b16b1

# 3) 如需同时回滚周报文档提交
git revert --no-edit 76da1a3

# 4) 推送
git push origin main
```

说明：
- 该方案保留历史，适合线上已同步场景。
- 若回滚后需要恢复，直接再对 revert 提交执行一次 revert 即可。

## 方案 B：仅回滚合并提交引入的变更

```bash
# 1) 拉取最新主干
git checkout main
git pull --ff-only origin main

# 2) 回滚 merge commit（主线父提交为 1）
git revert -m 1 --no-edit 0912625

# 3) 推送
git push origin main
```

说明：
- 该方案会整体撤销 PR #1 合入内容。
- 执行前建议先在预发布环境验证冲突与依赖影响。

## 风险检查清单

- 分享页接口契约：确认 /api/v1/share/{token} 返回结构是否与前端一致。
- URL 字段语义：确认 url/page_url/api_url 使用方是否受影响。
- 文档链接：确认 docs/system-design 相关引用是否仍可跳转。

## 一键演练（本地 dry-run）

```bash
# 创建演练分支，不污染 main
git checkout -b drill/rollback-20260426

# 演练回滚修复提交
git revert --no-commit d8b16b1

# 查看结果后撤销演练
git reset --hard HEAD
git checkout main
git branch -D drill/rollback-20260426
```
