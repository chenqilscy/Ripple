# Phase 13 发布说明（2026-04-27）

## 本轮完成项

1. 远端 staging 标准 bootstrap 与 smoke 已在 `fn.cky` 收口，`backend`、`frontend`、`yjs-bridge` 均通过真实 Docker 启动验证；
2. TD-04 已完成根因收口与 clean rerun，远端 Linux 客户端结果为 `1000 / 1000` 成功、`p95 175.797ms`、`p99 192.333ms`；
3. Redis / Neo4j / yjs-bridge 故障演练已全部通过，其中 yjs-bridge 的恢复缺陷已修复；
4. 全文检索 `10k` 节点基线已补测，`50` 次样本 `p95 66.4085ms`，满足 `< 300ms` 准入门槛。

## 本轮关键修复

1. `cmd/migrate` 已修复 BOM 与 SQL statement split 问题，空库迁移不再卡死；
2. frontend Docker build 已切到更稳的 npm mirror 配置，远端构建可重复；
3. `yjs-bridge` staging 启动链路已修复为：
   - 镜像改为 `golang:1.23-alpine`；
   - JWT 配置统一为 `RIPPLE_JWT_SECRET`；
   - Redis 配置统一为 `YJS_BRIDGE_REDIS_URL`。

## 当前状态

Phase 13 当前为绿灯。联调、性能、稳定性、运维回收、应用回滚、数据库 down/up 演练与 CI 检查均已完成。回滚验收记录见 `docs/dev/Phase13-rollback-acceptance-20260427.md`。

补充：`1000` 行批量导入 `5s` 响应门槛已于 `2026-04-27` 在 `fn.cky` 环境通过，结果见 `docs/dev/Phase13-batch-import-1000-baseline-20260427-213021.md`。

## 回滚验收补充

远端 GitHub clone 出现 TLS 握手失败，因此本次采用本地 `git archive` + `scp` 上传源码包的方式完成回滚验收。`a9834d0` 回滚态、`c6648a3` 恢复态与数据库 down/up 后均 smoke 全绿。