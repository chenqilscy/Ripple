# 图谱价值增强实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 LakeGraph.tsx 从"可视化工具"升级为"探索工具"，核心价值：发现（智能推荐+路径追溯）和规划（知识缺口分析）。

**Architecture:** 前端增量叠加，不破坏现有功能。新增 DiscoveryPanel、PathTracePanel、ClusterView、PlanningPanel 四个面板组件，以及 RecommendationEngine 状态管理器。后端按需实现推荐和路径分析接口。

**Tech Stack:** React 18 + TypeScript + React Three Fiber + D3-force + Three.js + SSE/WebSocket

---

## 文件结构

```
frontend/src/
├── components/graph/
│   ├── RecommendationEngine.ts    # 新增：推荐状态管理（useReducer）
│   ├── DiscoveryPanel.tsx         # 新增：发现面板（推荐列表+操作）
│   ├── PathTracePanel.tsx         # 新增：路径追溯面板
│   ├── ClusterView.tsx            # 新增：聚类视图（着色+聚焦）
│   └── PlanningPanel.tsx          # 新增：规划面板（缺口分析建议卡）
└── components/LakeGraph.tsx       # 修改：集成四个面板 + 推荐徽章

frontend/src/api/
├── types.ts                       # 修改：添加 Recommendation/PathResult/Cluster/PlanningSuggestion 类型
└── client.ts                      # 修改：添加图谱分析 API（路径/聚类/规划/推荐操作）

backend-go/
├── internal/graph/                # 新增目录：图谱分析服务
│   ├── engine.go                  # 推荐/路径/聚类/规划算法
│   └── api.go                     # REST + SSE 接口
└── 现有 handler 文件              # 修改：注册新路由
```

---

## Task 1: 类型定义

**Files:**
- Modify: `frontend/src/api/types.ts`
- Modify: `frontend/src/api/client.ts`

- [ ] **Step 1: 添加图谱分析类型到 types.ts**

在 `types.ts` 末尾添加：

```typescript
// ---- 图谱价值增强：推荐/路径/聚类/规划 ----
export type RecommendationStatus = 'pending' | 'accepted' | 'rejected' | 'ignored'

export interface Recommendation {
  id: string
  source_node_id: string
  target_node_id: string
  reason: string       // 推荐理由："两者都涉及系统可用性"
  confidence: number   // 置信度 0-1
  created_at: string
  status: RecommendationStatus
}

export interface PathNode {
  id: string
  title: string
  reason: string  // 该节点在路径中的作用
}

export interface PathEdge {
  from: string
  to: string
  similarity: number
  kind: string
}

export interface PathResult {
  source_id: string
  target_id: string
  nodes: PathNode[]
  edges: PathEdge[]
  total_steps: number
}

export interface Cluster {
  id: string
  label: string       // AI 生成的领域名称
  node_ids: string[]
  color: string
  bridge_node_ids: string[]  // 跨领域桥接节点
  density: number     // 领域饱满程度
}

export type PlanningSuggestionType = 'add_node' | 'connect' | 'explore'
export type PlanningPriority = 'high' | 'medium' | 'low'

export interface PlanningSuggestion {
  id: string
  type: PlanningSuggestionType
  title: string
  description: string
  priority: PlanningPriority
  related_node_ids: string[]
}
```

- [ ] **Step 2: 在 client.ts 中添加图谱分析 API**

在 `api` 对象的末尾（`importText` 之后）添加：

```typescript
  // ---- 图谱价值增强 ----
  // 路径追溯
  getPath(sourceId: string, targetId: string): Promise<PathResult> {
    return request('POST', '/api/v1/graph/path', { source_id: sourceId, target_id: targetId })
  },

  // 聚类分析
  getClusters(lakeId: string): Promise<{ clusters: Cluster[] }> {
    return request('GET', `/api/v1/lakes/${lakeId}/clusters`)
  },

  // 规划建议
  getPlanningSuggestions(lakeId: string): Promise<{ suggestions: PlanningSuggestion[] }> {
    return request('GET', `/api/v1/lakes/${lakeId}/planning`)
  },

  // 推荐列表
  getRecommendations(lakeId: string): Promise<{ recommendations: Recommendation[] }> {
    return request('GET', `/api/v1/lakes/${lakeId}/recommendations`)
  },

  // 推荐操作
  acceptRecommendation(id: string): Promise<void> {
    return request('POST', `/api/v1/recommendations/${id}/accept')
  },
  rejectRecommendation(id: string): Promise<void> {
    return request('POST', `/api/v1/recommendations/${id}/reject')
  },
  ignoreRecommendation(id: string): Promise<void> {
    return request('POST', `/api/v1/recommendations/${id}/ignore')
  },
```

- [ ] **Step 3: 提交类型定义**

```bash
cd "d:\work\local\Ripple" && git add "frontend/src/api/types.ts" "frontend/src/api/client.ts" && git commit -m "feat: add graph analysis types and API (recommendation/path/cluster/planning)"
```

---

## Task 2: RecommendationEngine 状态管理器

**Files:**
- Create: `frontend/src/components/graph/RecommendationEngine.ts`

- [ ] **Step 1: 创建 RecommendationEngine.ts**

```typescript
// RecommendationEngine.ts — 图谱推荐状态管理器
import { useReducer, useCallback, useEffect } from 'react'
import { api } from '../../api/client'
import type { Recommendation, PathResult, Cluster, PlanningSuggestion } from '../../api/types'

// ---- State ----
export interface GraphAnalysisState {
  // 发现
  recommendations: Recommendation[]
  recommendationsByNode: Map<string, Recommendation[]>  // nodeId -> 该节点的推荐
  loadingRecommendations: boolean

  // 路径追溯
  activePath: PathResult | null
  loadingPath: boolean

  // 聚类
  clusters: Cluster[]
  focusedClusterId: string | null
  loadingClusters: boolean

  // 规划
  planningSuggestions: PlanningSuggestion[]
  loadingPlanning: boolean

  // UI 状态
  activePanel: 'discovery' | 'path' | 'cluster' | 'planning' | null
}

type Action =
  | { type: 'SET_RECOMMENDATIONS'; recommendations: Recommendation[] }
  | { type: 'SET_LOADING_RECOMMENDATIONS'; loading: boolean }
  | { type: 'SET_ACTIVE_PATH'; path: PathResult | null }
  | { type: 'SET_LOADING_PATH'; loading: boolean }
  | { type: 'SET_CLUSTERS'; clusters: Cluster[] }
  | { type: 'SET_FOCUSED_CLUSTER'; id: string | null }
  | { type: 'SET_LOADING_CLUSTERS'; loading: boolean }
  | { type: 'SET_PLANNING'; suggestions: PlanningSuggestion[] }
  | { type: 'SET_LOADING_PLANNING'; loading: boolean }
  | { type: 'SET_ACTIVE_PANEL'; panel: GraphAnalysisState['activePanel'] }
  | { type: 'REMOVE_RECOMMENDATION'; id: string }
  | { type: 'UPDATE_RECOMMENDATION_STATUS'; id: string; status: Recommendation['status'] }

function buildRecommendationsByNode(recs: Recommendation[]): Map<string, Recommendation[]> {
  const map = new Map<string, Recommendation[]>()
  for (const r of recs) {
    if (!map.has(r.source_node_id)) map.set(r.source_node_id, [])
    if (!map.has(r.target_node_id)) map.set(r.target_node_id, [])
    map.get(r.source_node_id)!.push(r)
    map.get(r.target_node_id)!.push(r)
  }
  return map
}

function reducer(state: GraphAnalysisState, action: Action): GraphAnalysisState {
  switch (action.type) {
    case 'SET_RECOMMENDATIONS': {
      const byNode = buildRecommendationsByNode(action.recommendations)
      return { ...state, recommendations: action.recommendations, recommendationsByNode: byNode, loadingRecommendations: false }
    }
    case 'SET_LOADING_RECOMMENDATIONS':
      return { ...state, loadingRecommendations: action.loading }
    case 'SET_ACTIVE_PATH':
      return { ...state, activePath: action.path, loadingPath: false }
    case 'SET_LOADING_PATH':
      return { ...state, loadingPath: action.loading }
    case 'SET_CLUSTERS':
      return { ...state, clusters: action.clusters, loadingClusters: false }
    case 'SET_FOCUSED_CLUSTER':
      return { ...state, focusedClusterId: action.id }
    case 'SET_LOADING_CLUSTERS':
      return { ...state, loadingClusters: action.loading }
    case 'SET_PLANNING':
      return { ...state, planningSuggestions: action.suggestions, loadingPlanning: false }
    case 'SET_LOADING_PLANNING':
      return { ...state, loadingPlanning: action.loading }
    case 'SET_ACTIVE_PANEL':
      return { ...state, activePanel: action.panel }
    case 'REMOVE_RECOMMENDATION': {
      const recs = state.recommendations.filter(r => r.id !== action.id)
      return { ...state, recommendations: recs, recommendationsByNode: buildRecommendationsByNode(recs) }
    }
    case 'UPDATE_RECOMMENDATION_STATUS': {
      const recs = state.recommendations.map(r => r.id === action.id ? { ...r, status: action.status } : r)
      return { ...state, recommendations: recs, recommendationsByNode: buildRecommendationsByNode(recs) }
    }
    default:
      return state
  }
}

const initialState: GraphAnalysisState = {
  recommendations: [],
  recommendationsByNode: new Map(),
  loadingRecommendations: false,
  activePath: null,
  loadingPath: false,
  clusters: [],
  focusedClusterId: null,
  loadingClusters: false,
  planningSuggestions: [],
  loadingPlanning: false,
  activePanel: null,
}

// ---- Hook ----
export function useGraphAnalysis(lakeId: string) {
  const [state, dispatch] = useReducer(reducer, initialState)

  // 加载推荐
  const loadRecommendations = useCallback(async () => {
    dispatch({ type: 'SET_LOADING_RECOMMENDATIONS', loading: true })
    try {
      const { recommendations } = await api.getRecommendations(lakeId)
      dispatch({ type: 'SET_RECOMMENDATIONS', recommendations })
    } catch {
      dispatch({ type: 'SET_LOADING_RECOMMENDATIONS', loading: false })
    }
  }, [lakeId])

  // 加载聚类
  const loadClusters = useCallback(async () => {
    dispatch({ type: 'SET_LOADING_CLUSTERS', loading: true })
    try {
      const { clusters } = await api.getClusters(lakeId)
      dispatch({ type: 'SET_CLUSTERS', clusters })
    } catch {
      dispatch({ type: 'SET_LOADING_CLUSTERS', loading: false })
    }
  }, [lakeId])

  // 加载规划建议
  const loadPlanning = useCallback(async () => {
    dispatch({ type: 'SET_LOADING_PLANNING', loading: true })
    try {
      const { suggestions } = await api.getPlanningSuggestions(lakeId)
      dispatch({ type: 'SET_PLANNING', suggestions })
    } catch {
      dispatch({ type: 'SET_LOADING_PLANNING', loading: false })
    }
  }, [lakeId])

  // 计算路径
  const tracePath = useCallback(async (sourceId: string, targetId: string) => {
    dispatch({ type: 'SET_LOADING_PATH', loading: true })
    try {
      const path = await api.getPath(sourceId, targetId)
      dispatch({ type: 'SET_ACTIVE_PATH', path })
    } catch {
      dispatch({ type: 'SET_LOADING_PATH', loading: false })
    }
  }, [])

  // 接受推荐
  const acceptRecommendation = useCallback(async (rec: Recommendation) => {
    await api.acceptRecommendation(rec.id)
    // 创建边（source -> target）
    try {
      await api.createEdge(rec.source_node_id, rec.target_node_id, 'relates', rec.reason)
    } catch { /* 边可能已存在 */ }
    dispatch({ type: 'UPDATE_RECOMMENDATION_STATUS', id: rec.id, status: 'accepted' })
  }, [])

  // 拒绝推荐
  const rejectRecommendation = useCallback(async (id: string) => {
    await api.rejectRecommendation(id)
    dispatch({ type: 'REMOVE_RECOMMENDATION', id })
  }, [])

  // 忽略推荐
  const ignoreRecommendation = useCallback(async (id: string) => {
    await api.ignoreRecommendation(id)
    dispatch({ type: 'REMOVE_RECOMMENDATION', id })
  }, [])

  // 切换面板
  const setActivePanel = useCallback((panel: GraphAnalysisState['activePanel']) => {
    dispatch({ type: 'SET_ACTIVE_PANEL', panel })
    // 按需加载数据
    if (panel === 'discovery' && state.recommendations.length === 0 && !state.loadingRecommendations) {
      loadRecommendations()
    } else if (panel === 'cluster' && state.clusters.length === 0 && !state.loadingClusters) {
      loadClusters()
    } else if (panel === 'planning' && state.planningSuggestions.length === 0 && !state.loadingPlanning) {
      loadPlanning()
    }
  }, [state, loadRecommendations, loadClusters, loadPlanning])

  // 聚焦聚类
  const focusCluster = useCallback((id: string | null) => {
    dispatch({ type: 'SET_FOCUSED_CLUSTER', id })
  }, [])

  // 关闭路径
  const closePath = useCallback(() => {
    dispatch({ type: 'SET_ACTIVE_PATH', path: null })
  }, [])

  // 获取某节点的推荐数（用于徽章）
  const getRecommendationCount = useCallback((nodeId: string): number => {
    const recs = state.recommendationsByNode.get(nodeId) ?? []
    return recs.filter(r => r.status === 'pending').length
  }, [state.recommendationsByNode])

  return {
    state,
    loadRecommendations,
    loadClusters,
    loadPlanning,
    tracePath,
    acceptRecommendation,
    rejectRecommendation,
    ignoreRecommendation,
    setActivePanel,
    focusCluster,
    closePath,
    getRecommendationCount,
  }
}
```

- [ ] **Step 2: 提交 RecommendationEngine**

```bash
cd "d:\work\local\Ripple" && git add "frontend/src/components/graph/RecommendationEngine.ts" && git commit -m "feat: add RecommendationEngine state manager for graph analysis"
```

---

## Task 3: DiscoveryPanel 发现面板

**Files:**
- Create: `frontend/src/components/graph/DiscoveryPanel.tsx`

- [ ] **Step 1: 创建 DiscoveryPanel.tsx**

```typescript
// DiscoveryPanel.tsx — 发现面板：推荐列表 + 路径触发
import React from 'react'
import type { Recommendation, PathResult } from '../../api/types'

interface DiscoveryPanelProps {
  recommendations: Recommendation[]
  loading: boolean
  activePath: PathResult | null
  loadingPath: boolean
  onAccept: (rec: Recommendation) => void
  onReject: (id: string) => void
  onIgnore: (id: string) => void
  onTracePath: (sourceId: string, targetId: string) => void
  onClosePath: () => void
  onClose: () => void
}

const CONFIDENCE_LABEL: Record<string, string> = {
  high: '高置信',
  medium: '中置信',
  low: '低置信',
}

function confidenceLevel(conf: number): 'high' | 'medium' | 'low' {
  if (conf >= 0.7) return 'high'
  if (conf >= 0.4) return 'medium'
  return 'low'
}

const CONFIDENCE_COLOR: Record<string, string> = {
  high: '#52c41a',
  medium: '#faad14',
  low: '#8c8c8c',
}

export default function DiscoveryPanel({
  recommendations, loading, activePath, loadingPath,
  onAccept, onReject, onIgnore, onTracePath, onClosePath, onClose,
}: DiscoveryPanelProps) {
  const pending = recommendations.filter(r => r.status === 'pending')

  return (
    <div style={{
      position: 'absolute', top: 12, right: 70, width: 280,
      background: 'rgba(6,13,26,0.95)', border: '1px solid #2e8b90',
      borderRadius: 8, zIndex: 40, maxHeight: 420, overflowY: 'auto',
      boxShadow: '0 4px 20px rgba(0,0,0,0.5)',
    }}>
      {/* Header */}
      <div style={{
        padding: '10px 14px 8px', borderBottom: '1px solid rgba(46,139,144,0.3)',
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
      }}>
        <span style={{ color: '#9ec5ee', fontSize: 13, fontWeight: 600 }}>💡 发现关联</span>
        <button onClick={onClose} style={closeBtnStyle}>✕</button>
      </div>

      {/* Content */}
      <div style={{ padding: '8px 0' }}>
        {loading && <div style={emptyStyle}>分析中...</div>}
        {!loading && pending.length === 0 && (
          <div style={emptyStyle}>暂无新发现<br /><span style={{ fontSize: 11, opacity: 0.6 }}>继续积累，关联会逐渐浮现</span></div>
        )}
        {!loading && pending.map(rec => {
          const lvl = confidenceLevel(rec.confidence)
          return (
            <div key={rec.id} style={{
              margin: '0 10px 8px', padding: '8px 10px',
              background: 'rgba(46,139,144,0.1)', borderRadius: 6, border: '1px solid rgba(46,139,144,0.2)',
            }}>
              <div style={{ fontSize: 11, color: CONFIDENCE_COLOR[lvl], marginBottom: 4 }}>
                {CONFIDENCE_LABEL[lvl]} · {Math.round(rec.confidence * 100)}%
              </div>
              <div style={{ fontSize: 12, color: '#c0d8f0', marginBottom: 6, lineHeight: 1.5 }}>
                {rec.reason}
              </div>
              {/* Actions */}
              <div style={{ display: 'flex', gap: 4 }}>
                <button onClick={() => onAccept(rec)} style={{ ...btnStyle, background: 'rgba(82,196,26,0.2)', color: '#52c41a', borderColor: 'rgba(82,196,26,0.4)' }}>
                  建立关联
                </button>
                <button onClick={() => onTracePath(rec.source_node_id, rec.target_node_id)} style={{ ...btnStyle, background: 'rgba(46,139,144,0.2)', color: '#2e8b90' }}>
                  路径
                </button>
                <button onClick={() => onIgnore(rec.id)} style={{ ...btnStyle, color: '#666', fontSize: 10 }}>
                  忽略
                </button>
              </div>
            </div>
          )
        })}
      </div>

      {/* Path Result */}
      {activePath && (
        <div style={{ borderTop: '1px solid rgba(46,139,144,0.3)', padding: '8px 12px' }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
            <span style={{ color: '#2e8b90', fontSize: 12 }}>路径追溯 · {activePath.total_steps} 步</span>
            <button onClick={onClosePath} style={{ ...closeBtnStyle, fontSize: 10 }}>关闭</button>
          </div>
          <div style={{ fontSize: 11, color: '#9ec5ee', lineHeight: 1.8 }}>
            {activePath.nodes.map((n, i) => (
              <span key={n.id}>
                <span style={{ color: '#fff' }}>{n.title || n.id.slice(0, 8)}</span>
                {i < activePath.nodes.length - 1 && (
                  <span style={{ color: '#2e8b90', margin: '0 4px' }}>→</span>
                )}
              </span>
            ))}
          </div>
        </div>
      )}
      {loadingPath && <div style={{ ...emptyStyle, borderTop: '1px solid rgba(46,139,144,0.3)', padding: '8px 12px' }}>计算路径中...</div>}
    </div>
  )
}

const closeBtnStyle: React.CSSProperties = {
  background: 'transparent', border: 'none', color: '#666', cursor: 'pointer', padding: '0 4px', fontSize: 12,
}

const emptyStyle: React.CSSProperties = {
  padding: '20px 12px', textAlign: 'center' as const, color: '#4a6a8e', fontSize: 12, lineHeight: 1.8,
}

const btnStyle: React.CSSProperties = {
  flex: 1, padding: '3px 0', fontSize: 11, cursor: 'pointer',
  borderRadius: 4, border: '1px solid rgba(46,139,144,0.3)',
  background: 'transparent', color: '#9ec5ee',
}
```

- [ ] **Step 2: 提交 DiscoveryPanel**

```bash
cd "d:\work\local\Ripple" && git add "frontend/src/components/graph/DiscoveryPanel.tsx" && git commit -m "feat: add DiscoveryPanel component for recommendation display"
```

---

## Task 4: PathTracePanel 路径追溯面板

**Files:**
- Create: `frontend/src/components/graph/PathTracePanel.tsx`

- [ ] **Step 1: 创建 PathTracePanel.tsx**

```typescript
// PathTracePanel.tsx — 路径追溯面板：展示两节点间的关联链路
import React from 'react'
import type { PathResult } from '../../api/types'

interface PathTracePanelProps {
  path: PathResult | null
  loading: boolean
  onTracePath: (sourceId: string, targetId: string) => void
  onClose: () => void
}

export default function PathTracePanel({ path, loading, onTracePath, onClose }: PathTracePanelProps) {
  if (!path && !loading) return null

  return (
    <div style={{
      position: 'absolute', top: 12, right: 70, width: 300,
      background: 'rgba(6,13,26,0.96)', border: '1px solid #2e8b90',
      borderRadius: 8, zIndex: 40, boxShadow: '0 4px 20px rgba(0,0,0,0.5)',
    }}>
      <div style={{
        padding: '10px 14px 8px', borderBottom: '1px solid rgba(46,139,144,0.3)',
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
      }}>
        <span style={{ color: '#9ec5ee', fontSize: 13, fontWeight: 600 }}>🔍 路径追溯</span>
        <button onClick={onClose} style={closeBtnStyle}>✕</button>
      </div>

      {loading && (
        <div style={{ padding: '20px 12px', textAlign: 'center', color: '#4a6a8e' }}>
          计算路径中...
        </div>
      )}

      {path && (
        <div style={{ padding: '8px 12px' }}>
          <div style={{ fontSize: 11, color: '#4a6a8e', marginBottom: 8 }}>
            {path.total_steps} 步关联链路
          </div>
          {path.nodes.map((n, i) => (
            <div key={n.id} style={{ display: 'flex', alignItems: 'center', marginBottom: 8 }}>
              {/* 步骤圆点 */}
              <div style={{
                width: 20, height: 20, borderRadius: '50%',
                background: i === 0 || i === path.nodes.length - 1 ? '#2e8b90' : 'rgba(46,139,144,0.3)',
                color: '#fff', fontSize: 10, display: 'flex', alignItems: 'center', justifyContent: 'center',
                marginRight: 8, flexShrink: 0,
              }}>
                {i + 1}
              </div>
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ fontSize: 12, color: '#c0d8f0', whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>
                  {n.title || n.id.slice(0, 12)}
                </div>
                {n.reason && (
                  <div style={{ fontSize: 10, color: '#4a6a8e', marginTop: 2 }}>
                    {n.reason}
                  </div>
                )}
              </div>
            </div>
          ))}
        </div>
      )}
    </div>
  )
}

const closeBtnStyle: React.CSSProperties = {
  background: 'transparent', border: 'none', color: '#666', cursor: 'pointer', padding: '0 4px', fontSize: 12,
}
```

- [ ] **Step 2: 提交 PathTracePanel**

```bash
cd "d:\work\local\Ripple" && git add "frontend/src/components/graph/PathTracePanel.tsx" && git commit -m "feat: add PathTracePanel for path visualization"
```

---

## Task 5: ClusterView 聚类视图

**Files:**
- Create: `frontend/src/components/graph/ClusterView.tsx`
- Modify: `frontend/src/components/LakeGraph.tsx:1-1007` (集成聚类高亮)

- [ ] **Step 1: 创建 ClusterView.tsx**

```typescript
// ClusterView.tsx — 聚类视图：颜色区分领域 + 聚焦/重置
import React from 'react'
import type { Cluster } from '../../api/types'

interface ClusterViewProps {
  clusters: Cluster[]
  focusedClusterId: string | null
  loading: boolean
  onFocus: (id: string | null) => void
  onRefresh: () => void
  onClose: () => void
}

// 预定义聚类颜色（与品牌色协调）
const CLUSTER_COLORS = [
  '#2e8b90', '#4a8eff', '#52c41a', '#faad14',
  '#f5222d', '#722ed1', '#eb2f96', '#13c2c2',
]

export default function ClusterView({ clusters, focusedClusterId, loading, onFocus, onRefresh, onClose }: ClusterViewProps) {
  return (
    <div style={{
      position: 'absolute', bottom: 60, left: 12, width: 220,
      background: 'rgba(6,13,26,0.95)', border: '1px solid #2a4a7e',
      borderRadius: 8, zIndex: 40, maxHeight: 320, overflowY: 'auto',
      boxShadow: '0 4px 20px rgba(0,0,0,0.5)',
    }}>
      <div style={{
        padding: '8px 12px 6px', borderBottom: '1px solid rgba(46,74,126,0.3)',
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
      }}>
        <span style={{ color: '#9ec5ee', fontSize: 12, fontWeight: 600 }}>🗂 知识领域</span>
        <div style={{ display: 'flex', gap: 4 }}>
          <button onClick={onRefresh} title="刷新聚类" style={iconBtn}>↻</button>
          <button onClick={() => onFocus(null)} title="显示全部" style={iconBtn}>⊡</button>
          <button onClick={onClose} style={iconBtn}>✕</button>
        </div>
      </div>

      {loading && (
        <div style={{ padding: '16px 12px', textAlign: 'center', color: '#4a6a8e', fontSize: 12 }}>
          分析中...
        </div>
      )}

      {!loading && clusters.length === 0 && (
        <div style={{ padding: '16px 12px', textAlign: 'center', color: '#4a6a8e', fontSize: 12 }}>
          节点数量不足<br /><span style={{ fontSize: 11, opacity: 0.6 }}>至少需要 5 个节点才能聚类</span>
        </div>
      )}

      {!loading && clusters.map((cluster, i) => {
        const color = cluster.color || CLUSTER_COLORS[i % CLUSTER_COLORS.length]
        const isFocused = focusedClusterId === cluster.id
        return (
          <div
            key={cluster.id}
            onClick={() => onFocus(isFocused ? null : cluster.id)}
            style={{
              padding: '7px 12px', cursor: 'pointer',
              background: isFocused ? `${color}22` : 'transparent',
              borderBottom: '1px solid rgba(46,74,126,0.2)',
              transition: 'background 0.2s',
            }}
          >
            <div style={{ display: 'flex', alignItems: 'center', gap: 6, marginBottom: 3 }}>
              <div style={{ width: 8, height: 8, borderRadius: '50%', background: color, flexShrink: 0 }} />
              <span style={{ fontSize: 12, color: isFocused ? '#fff' : '#c0d8f0', fontWeight: isFocused ? 600 : 400, flex: 1, minWidth: 0, whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>
                {cluster.label}
              </span>
              <span style={{ fontSize: 10, color: '#4a6a8e' }}>{cluster.node_ids.length}</span>
            </div>
            {cluster.bridge_node_ids.length > 0 && (
              <div style={{ fontSize: 10, color: '#faad14', marginLeft: 14 }}>
                🔗 跨领域节点 {cluster.bridge_node_ids.length} 个
              </div>
            )}
          </div>
        )
      })}
    </div>
  )
}

const iconBtn: React.CSSProperties = {
  background: 'transparent', border: 'none', color: '#666', cursor: 'pointer',
  padding: '0 3px', fontSize: 11, lineHeight: 1,
}
```

- [ ] **Step 2: 在 LakeGraph.tsx 中添加聚类高亮逻辑**

在 `GraphScene` 组件中，找到 `highlightedIds` 的 `useMemo`，在其后添加聚类高亮逻辑。同时需要将 `clusters` 和 `focusedClusterId` 作为 prop 传入 GraphScene。

（具体修改见 Task 8：集成所有面板到 LakeGraph）

- [ ] **Step 3: 提交 ClusterView**

```bash
cd "d:\work\local\Ripple" && git add "frontend/src/components/graph/ClusterView.tsx" && git commit -m "feat: add ClusterView for knowledge domain visualization"
```

---

## Task 6: PlanningPanel 规划面板

**Files:**
- Create: `frontend/src/components/graph/PlanningPanel.tsx`

- [ ] **Step 1: 创建 PlanningPanel.tsx**

```typescript
// PlanningPanel.tsx — 规划面板：知识缺口分析 + 行动建议
import React from 'react'
import type { PlanningSuggestion } from '../../api/types'

interface PlanningPanelProps {
  suggestions: PlanningSuggestion[]
  loading: boolean
  onAccept: (s: PlanningSuggestion) => void  // 创建节点或建立关联
  onRefresh: () => void
  onClose: () => void
}

const PRIORITY_COLOR: Record<string, string> = {
  high: '#f5222d',
  medium: '#faad14',
  low: '#8c8c8c',
}

const TYPE_LABEL: Record<string, string> = {
  add_node: '添加节点',
  connect: '建立关联',
  explore: '深入探索',
}

export default function PlanningPanel({ suggestions, loading, onAccept, onRefresh, onClose }: PlanningPanelProps) {
  const byPriority = ['high', 'medium', 'low']

  return (
    <div style={{
      position: 'absolute', top: 12, right: 70, width: 300,
      background: 'rgba(6,13,26,0.95)', border: '1px solid #2e8b90',
      borderRadius: 8, zIndex: 40, maxHeight: 480, overflowY: 'auto',
      boxShadow: '0 4px 20px rgba(0,0,0,0.5)',
    }}>
      <div style={{
        padding: '10px 14px 8px', borderBottom: '1px solid rgba(46,139,144,0.3)',
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
      }}>
        <span style={{ color: '#9ec5ee', fontSize: 13, fontWeight: 600 }}>📋 规划建议</span>
        <div style={{ display: 'flex', gap: 4 }}>
          <button onClick={onRefresh} title="重新分析" style={iconBtn}>↻</button>
          <button onClick={onClose} style={iconBtn}>✕</button>
        </div>
      </div>

      {loading && (
        <div style={{ padding: '20px 12px', textAlign: 'center', color: '#4a6a8e' }}>
          分析知识结构中...
        </div>
      )}

      {!loading && suggestions.length === 0 && (
        <div style={{ padding: '20px 12px', textAlign: 'center', color: '#4a6a8e', lineHeight: 1.8 }}>
          暂无规划建议<br />
          <span style={{ fontSize: 11, opacity: 0.6 }}>继续积累节点和关联，规划建议会更精准</span>
        </div>
      )}

      {!loading && byPriority.map(priority => {
        const items = suggestions.filter(s => s.priority === priority)
        if (items.length === 0) return null
        return (
          <div key={priority} style={{ padding: '0 0 8px' }}>
            <div style={{
              padding: '4px 14px', fontSize: 10,
              color: PRIORITY_COLOR[priority],
              textTransform: 'uppercase', letterSpacing: 1,
            }}>
              {priority === 'high' ? '⚠ 高优先级' : priority === 'medium' ? '中优先级' : '低优先级'}
            </div>
            {items.map(s => (
              <div key={s.id} style={{
                margin: '0 10px 6px', padding: '8px 10px',
                background: 'rgba(0,0,0,0.2)', borderRadius: 6,
                border: `1px solid ${PRIORITY_COLOR[priority]}22`,
              }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: 6, marginBottom: 4 }}>
                  <span style={{
                    fontSize: 10, padding: '1px 5px', borderRadius: 3,
                    background: `${PRIORITY_COLOR[priority]}22`, color: PRIORITY_COLOR[priority],
                  }}>
                    {TYPE_LABEL[s.type]}
                  </span>
                  <span style={{ fontSize: 12, color: '#c0d8f0', fontWeight: 500 }}>
                    {s.title}
                  </span>
                </div>
                <div style={{ fontSize: 11, color: '#4a6a8e', marginBottom: 6, lineHeight: 1.5 }}>
                  {s.description}
                </div>
                <button
                  onClick={() => onAccept(s)}
                  style={{
                    width: '100%', padding: '4px 0', fontSize: 11, cursor: 'pointer',
                    borderRadius: 4, border: `1px solid ${PRIORITY_COLOR[priority]}55`,
                    background: `${PRIORITY_COLOR[priority]}11`, color: PRIORITY_COLOR[priority],
                  }}
                >
                  一键采纳
                </button>
              </div>
            ))}
          </div>
        )
      })}
    </div>
  )
}

const iconBtn: React.CSSProperties = {
  background: 'transparent', border: 'none', color: '#666', cursor: 'pointer',
  padding: '0 3px', fontSize: 11,
}
```

- [ ] **Step 2: 提交 PlanningPanel**

```bash
cd "d:\work\local\Ripple" && git add "frontend/src/components/graph/PlanningPanel.tsx" && git commit -m "feat: add PlanningPanel for knowledge gap analysis"
```

---

## Task 7: 节点推荐徽章

**Files:**
- Modify: `frontend/src/components/LakeGraph.tsx` — `AnimatedNode` 组件

- [ ] **Step 1: 在 AnimatedNode 中添加推荐徽章**

在 `AnimatedNode` 的 `mesh` 内部，找到悬停 tooltip 的 `<Html>` 之后，添加推荐徽章：

```typescript
// 在 {hovered && !selected && (...)} tooltip 后面添加：
{/* 发现徽章：pending 推荐数量 */}
{recCount > 0 && !selected && (
  <mesh position={[5, 5, 0]}>
    <sphereGeometry args={[2.5, 6, 6]} />
    <meshBasicMaterial color="#faad14" />
  </mesh>
)}
```

同时在 `AnimNodeProps` 中添加 `recCount: number` prop。

- [ ] **Step 2: 在 GraphScene 调用 AnimatedNode 处传入 recCount**

在 `displayNodes.map` 中，找到 `<AnimatedNode ... />`，添加：

```typescript
<AnimatedNode
  ...
  recCount={recCountByNode.get(node.id) ?? 0}
/>
```

其中 `recCountByNode` 是从 `RecommendationEngine` 传入 GraphScene 的 Map。

- [ ] **Step 3: 提交徽章功能**

```bash
cd "d:\work\local\Ripple" && git add "frontend/src/components/LakeGraph.tsx" && git commit -m "feat: add recommendation badge on graph nodes"
```

---

## Task 8: 集成所有面板到 LakeGraph

**Files:**
- Modify: `frontend/src/components/LakeGraph.tsx`

- [ ] **Step 1: 修改 LakeGraphProps 导出类型**

在 `LakeGraphProps` 接口末尾添加：

```typescript
/** 图谱分析引擎（由 useGraphAnalysis 创建） */
graphAnalysis?: ReturnType<typeof import('./graph/RecommendationEngine').useGraphAnalysis>
```

（注意：为了避免循环依赖，改为直接在 props 中传入所需数据。）

更优方案：在 `LakeGraphProps` 中直接传入分析相关的数据和回调：

```typescript
// 追加到 LakeGraphProps:
/** 节点推荐数 Map（nodeId -> pending 推荐数），用于徽章 */
recCountByNode?: Map<string, number>
/** 发现面板 */
showDiscovery?: boolean
recommendations?: import('../api/types').Recommendation[]
loadingRecommendations?: boolean
activePath?: import('../api/types').PathResult | null
loadingPath?: boolean
onAcceptRec?: (rec: import('../api/types').Recommendation) => void
onIgnoreRec?: (id: string) => void
onTracePath?: (src: string, dst: string) => void
onClosePath?: () => void
/** 聚类 */
showCluster?: boolean
clusters?: import('../api/types').Cluster[]
focusedClusterId?: string | null
loadingClusters?: boolean
onFocusCluster?: (id: string | null) => void
/** 规划 */
showPlanning?: boolean
planningSuggestions?: import('../api/types').PlanningSuggestion[]
loadingPlanning?: boolean
```

- [ ] **Step 2: 修改 LakeGraph 组件，渲染四个面板**

在 `<Canvas>` 元素之后、`<div>` 结束标签之前，添加四个面板：

```typescript
{/* === 发现面板 === */}
{showDiscovery && (
  <DiscoveryPanel
    recommendations={recommendations ?? []}
    loading={loadingRecommendations ?? false}
    activePath={activePath ?? null}
    loadingPath={loadingPath ?? false}
    onAccept={onAcceptRec ?? (() => {})}
    onReject={onIgnoreRec ?? (() => {})}
    onIgnore={onIgnoreRec ?? (() => {})}
    onTracePath={onTracePath ?? (() => {})}
    onClosePath={onClosePath ?? (() => {})}
    onClose={() => { /* 调用关闭回调 */ }}
  />
)}

{/* === 路径追溯面板 === */}
{!showDiscovery && activePath && (
  <PathTracePanel
    path={activePath}
    loading={loadingPath ?? false}
    onTracePath={onTracePath ?? (() => {})}
    onClose={onClosePath ?? (() => {})}
  />
)}

{/* === 聚类视图 === */}
{showCluster && (
  <ClusterView
    clusters={clusters ?? []}
    focusedClusterId={focusedClusterId ?? null}
    loading={loadingClusters ?? false}
    onFocus={onFocusCluster ?? (() => {})}
    onRefresh={() => { /* 刷新聚类 */ }}
    onClose={() => { /* 关闭聚类 */ }}
  />
)}

{/* === 规划面板 === */}
{showPlanning && (
  <PlanningPanel
    suggestions={planningSuggestions ?? []}
    loading={loadingPlanning ?? false}
    onAccept={() => { /* 一键采纳建议 */ }}
    onRefresh={() => { /* 刷新规划 */ }}
    onClose={() => { /* 关闭规划 */ }}
  />
)}
```

- [ ] **Step 3: 在缩放控制按钮组旁添加面板切换按钮**

在缩放按钮组下方或上方，添加四个面板切换入口按钮（发现/聚类/规划 + 收起）：

```typescript
<div style={{ position: 'absolute', top: 12, left: 12, zIndex: 10, display: 'flex', gap: 4 }}>
  <button onClick={() => setShowDiscovery(v => !v)} style={{
    background: showDiscovery ? 'rgba(46,139,144,0.3)' : 'rgba(0,0,0,0.6)',
    border: '1px solid #2e8b90', color: '#9ec5ee', borderRadius: 4,
    padding: '3px 8px', fontSize: 11, cursor: 'pointer',
  }}>
    💡 发现
    {pendingCount > 0 && <span style={{ marginLeft: 4, background: '#faad14', borderRadius: '50%', width: 14, height: 14, display: 'inline-flex', alignItems: 'center', justifyContent: 'center', fontSize: 9, color: '#000' }}>{pendingCount}</span>}
  </button>
  <button onClick={() => setShowCluster(v => !v)} style={panelToggleBtnStyle(showCluster)}>🗂 领域</button>
  <button onClick={() => setShowPlanning(v => !v)} style={panelToggleBtnStyle(showPlanning)}>📋 规划</button>
</div>
```

- [ ] **Step 4: 添加聚类节点高亮到 GraphScene**

在 `GraphScene` 的 `highlightedIds` useMemo 之后，添加聚类高亮：

```typescript
const clusterHighlightedIds = useMemo(() => {
  if (!clusters || clusters.length === 0 || !focusedClusterId) return new Set<string>()
  const cluster = clusters.find(c => c.id === focusedClusterId)
  if (!cluster) return new Set<string>()
  return new Set(cluster.node_ids)
}, [clusters, focusedClusterId])

const dimmedIds = useMemo(() => {
  if (!focusedClusterId) return new Set<string>()
  const allNodeIds = new Set(displayNodes.map(n => n.id))
  const highlighted = clusterHighlightedIds
  const dimmed = new Set<string>()
  for (const id of allNodeIds) if (!highlighted.has(id)) dimmed.add(id)
  return dimmed
}, [displayNodes, clusterHighlightedIds, focusedClusterId])
```

在 `AnimatedNode` 调用中添加 `dimmed` prop，节点透明度降低 0.3（当在 dimmed 中且存在 focusedClusterId 时）。

- [ ] **Step 5: 提交集成**

```bash
cd "d:\work\local\Ripple" && git add "frontend/src/components/LakeGraph.tsx" && git commit -m "feat: integrate all graph analysis panels into LakeGraph"
```

---

## Task 9: 后端图谱分析接口（最小实现）

**Files:**
- Create: `backend-go/internal/graph/engine.go`
- Modify: `backend-go/cmd/.../main.go`（注册路由）
- Modify: `backend-go/internal/.../handler_graph.go`（新建或扩展）

- [ ] **Step 1: 创建图谱分析引擎 engine.go**

```go
// backend-go/internal/graph/engine.go
package graph

import (
  "math"
  "sort"
)

// ---- Recommendation ----

type Recommendation struct {
  ID            string  `json:"id"`
  SourceNodeID  string  `json:"source_node_id"`
  TargetNodeID  string  `json:"target_node_id"`
  Reason        string  `json:"reason"`
  Confidence    float64 `json:"confidence"`
  CreatedAt     string  `json:"created_at"`
  Status        string  `json:"status"` // pending, accepted, rejected, ignored
}

// SimpleContentSimilarity 基于节点内容关键词重叠计算相似度（临时实现）
// 后续替换为 LLM 分析或 Embedding 余弦相似度
func SimpleContentSimilarity(contentA, contentB string) float64 {
  aWords := wordSet(contentA)
  bWords := wordSet(contentB)
  if len(aWords) == 0 || len(bWords) == 0 {
    return 0
  }
  intersection := 0
  for w := range aWords {
    if bWords[w] {
      intersection++
    }
  }
  union := float64(len(aWords) + len(bWords) - intersection)
  return float64(intersection) / union
}

func wordSet(s string) map[string]bool {
  words := make(map[string]bool)
  prev := rune(0)
  cur := []rune{}
  for _, r := range s {
    if (r >= 'a' && r <= 'z') || (r >= 'A' && r <= 'Z') || (r >= '0' && r <= '9') || r >= 0x4e00 {
      cur = append(cur, r)
    } else {
      if len(cur) >= 2 {
        words[string(cur)] = true
      }
      cur = cur[:0]
    }
    prev = r
  }
  if len(cur) >= 2 {
    words[string(cur)] = true
  }
  _ = prev
  return words
}

// GenerateRecommendations 扫描节点对，找出潜在关联
func GenerateRecommendations(nodes []struct {
  ID      string
  Content string
}, existingEdges map[[2]string]bool) []Recommendation {
  var recs []Recommendation
  THRESHOLD := 0.15
  for i := 0; i < len(nodes); i++ {
    for j := i + 1; j < len(nodes); j++ {
      key := [2]string{nodes[i].ID, nodes[j].ID}
      if existingEdges[key] {
        continue // 已有边，跳过
      }
      sim := SimpleContentSimilarity(nodes[i].Content, nodes[j].Content)
      if sim >= THRESHOLD {
        recs = append(recs, Recommendation{
          ID:           fmt.Sprintf("rec-%d", len(recs)),
          SourceNodeID: nodes[i].ID,
          TargetNodeID: nodes[j].ID,
          Reason:       fmt.Sprintf("内容相似度 %.0f%%", sim*100),
          Confidence:   math.Min(1, sim*2), // 映射到 0-1
          CreatedAt:    time.Now().Format(time.RFC3339),
          Status:       "pending",
        })
      }
    }
  }
  // 按置信度降序
  sort.Slice(recs, func(i, j int) bool {
    return recs[i].Confidence > recs[j].Confidence
  })
  return recs
}
```

- [ ] **Step 2: 创建图谱分析 handler**

在 `backend-go/internal/` 下创建或扩展 handler，提供 REST 接口：

```go
// backend-go/internal/graph/handler.go
package graph

import (
  "encoding/json"
  "net/http"

  "github.com/gin-gonic/gin"
)

func RegisterRoutes(r *gin.Engine) {
  g := r.Group("/api/v1/lakes/:lake_id/graph")
  g.GET("/recommendations", handleGetRecommendations)
  g.POST("/path", handleGetPath)
  g.GET("/clusters", handleGetClusters)
  g.GET("/planning", handleGetPlanningSuggestions)
}

func handleGetRecommendations(c *gin.Context) {
  lakeID := c.Param("lake_id")
  nodes := getLakeNodes(lakeID)  // 从 DB 获取节点
  edges := getLakeEdges(lakeID)  // 从 DB 获取边
  edgeSet := buildEdgeSet(edges)
  recs := GenerateRecommendations(nodes, edgeSet)
  c.JSON(http.StatusOK, gin.H{"recommendations": recs})
}

func handleGetPath(c *gin.Context) {
  var body struct {
    SourceID string `json:"source_id"`
    TargetID string `json:"target_id"`
  }
  if err := c.ShouldBindJSON(&body); err != nil {
    c.JSON(http.StatusBadRequest, gin.H{"error": "invalid request"})
    return
  }
  lakeID := c.Param("lake_id")
  path := FindPath(lakeID, body.SourceID, body.TargetID)
  c.JSON(http.StatusOK, path)
}

func handleGetClusters(c *gin.Context) {
  lakeID := c.Param("lake_id")
  nodes := getLakeNodes(lakeID)
  edges := getLakeEdges(lakeID)
  clusters := SimpleClustering(nodes, edges)
  c.JSON(http.StatusOK, gin.H{"clusters": clusters})
}

func handleGetPlanningSuggestions(c *gin.Context) {
  lakeID := c.Param("lake_id")
  nodes := getLakeNodes(lakeID)
  edges := getLakeEdges(lakeID)
  suggestions := AnalyzeKnowledgeGaps(nodes, edges)
  c.JSON(http.StatusOK, gin.H{"suggestions": suggestions})
}
```

- [ ] **Step 3: 添加路径/聚类/规划基础算法**

在 `engine.go` 中补充：

```go
// FindPath 简单的 BFS 最短路径（节点数少时够用）
func FindPath(lakeID, sourceID, targetID string) PathResult {
  edges := getLakeEdges(lakeID)
  adj := buildAdjList(edges)
  path := bfsPath(adj, sourceID, targetID)
  // path 为 []string 节点 ID 列表
  nodes := getNodesByIDs(path)
  return PathResult{
    SourceID:   sourceID,
    TargetID:   targetID,
    Nodes:      pathNodesToPathNodes(nodes),
    Edges:      extractEdgesAlongPath(path, edges),
    TotalSteps: len(path) - 1,
  }
}

// SimpleClustering 基于连通分量的简单聚类（后期替换为语义聚类）
func SimpleClustering(nodes []struct{ID, Content string}, edges []struct{Src, Dst string}) []Cluster {
  adj := buildAdjList(edges)
  visited := make(map[string]bool)
  var clusters []Cluster
  colors := []string{"#2e8b90", "#4a8eff", "#52c41a", "#faad14"}
  for _, n := range nodes {
    if visited[n.ID] {
      continue
    }
    component := bfsComponent(adj, n.ID, visited)
    clusters = append(clusters, Cluster{
      ID:      fmt.Sprintf("cluster-%d", len(clusters)),
      Label:   fmt.Sprintf("领域 %d", len(clusters)+1),
      NodeIDs: component,
      Color:   colors[len(clusters)%len(colors)],
      Density: float64(len(edges)) / float64(len(nodes)+1),
    })
  }
  return clusters
}

// AnalyzeKnowledgeGaps 孤岛检测 + 语义空白分析
func AnalyzeKnowledgeGaps(nodes []struct{ID, Content string}, edges []struct{Src, Dst string}) []PlanningSuggestion {
  adj := buildAdjList(edges)
  var suggestions []PlanningSuggestion
  // 孤岛检测
  for _, n := range nodes {
    if len(adj[n.ID]) == 0 {
      suggestions = append(suggestions, PlanningSuggestion{
        ID:          fmt.Sprintf("plan-%d", len(suggestions)),
        Type:        "explore",
        Title:       "孤立节点：" + n.ID[:8],
        Description: "该节点没有关联，考虑与其他节点建立关联",
        Priority:    "medium",
      })
    }
  }
  return suggestions
}
```

- [ ] **Step 4: 注册路由并提交**

在后端 main.go 中添加路由注册并提交：

```bash
cd "d:\work\local\Ripple/backend-go" && go build ./... && git add "internal/graph/" && git commit -m "feat: add graph analysis engine (recommendation/path/cluster/planning)"
```

---

## Task 10: 推荐徽章数据流（父组件集成）

**Files:**
- Modify: 父组件（如 LakePage.tsx / 使用 LakeGraph 的页面）

- [ ] **Step 1: 在父组件中接入 useGraphAnalysis**

在父组件中：

```typescript
import { useGraphAnalysis } from '../components/graph/RecommendationEngine'

// 在 LakeGraph 调用处传入分析数据：
const {
  state,
  setActivePanel,
  focusCluster,
  tracePath,
  acceptRecommendation,
  ignoreRecommendation,
  closePath,
  getRecommendationCount,
} = useGraphAnalysis(lakeId)
```

- [ ] **Step 2: 传入 LakeGraph props**

```typescript
<LakeGraph
  nodes={nodes}
  edges={edges}
  recCountByNode={new Map(
    nodes.map(n => [n.id, getRecommendationCount(n.id)] as [string, number])
  )}
  showDiscovery={state.activePanel === 'discovery'}
  recommendations={state.recommendations}
  loadingRecommendations={state.loadingRecommendations}
  activePath={state.activePath}
  loadingPath={state.loadingPath}
  onAcceptRec={acceptRecommendation}
  onIgnoreRec={ignoreRecommendation}
  onTracePath={tracePath}
  onClosePath={closePath}
  showCluster={state.activePanel === 'cluster'}
  clusters={state.clusters}
  focusedClusterId={state.focusedClusterId}
  loadingClusters={state.loadingClusters}
  onFocusCluster={focusCluster}
  showPlanning={state.activePanel === 'planning'}
  planningSuggestions={state.planningSuggestions}
  loadingPlanning={state.loadingPlanning}
/>
```

- [ ] **Step 3: 添加面板切换按钮事件**

在父组件的工具栏中添加三个按钮：

```typescript
<button onClick={() => setActivePanel(state.activePanel === 'discovery' ? null : 'discovery')}>💡 发现</button>
<button onClick={() => setActivePanel(state.activePanel === 'cluster' ? null : 'cluster')}>🗂 领域</button>
<button onClick={() => setActivePanel(state.activePanel === 'planning' ? null : 'planning')}>📋 规划</button>
```

- [ ] **Step 4: 提交父组件集成**

```bash
git add "frontend/src/pages/..." && git commit -m "feat: integrate graph analysis panels in lake page"
```

---

## 实施顺序

1. **Task 1** — 类型定义（无依赖，最先完成）
2. **Task 2** — RecommendationEngine 状态管理（依赖 Task 1）
3. **Task 3** — DiscoveryPanel（依赖 Task 1、2）
4. **Task 4** — PathTracePanel（依赖 Task 1）
5. **Task 5** — ClusterView（依赖 Task 1）
6. **Task 6** — PlanningPanel（依赖 Task 1）
7. **Task 7** — 节点推荐徽章（依赖 Task 1）
8. **Task 8** — 集成到 LakeGraph（依赖 Task 3、4、5、6、7）
9. **Task 9** — 后端接口（独立进行，与前端并行）
10. **Task 10** — 父组件集成（依赖 Task 8 + 后端接口）

---

## 验证检查点

每个 Task 完成后验证：

| Task | 验证点 |
|------|--------|
| 1 | TypeScript 编译无错误 `cd frontend && npx tsc --noEmit` |
| 2 | RecommendationEngine hook 可正常调用 |
| 3 | DiscoveryPanel 渲染正确，推荐列表可展示 |
| 4 | PathTracePanel 渲染路径节点 |
| 5 | ClusterView 显示领域列表 |
| 6 | PlanningPanel 显示优先级建议卡片 |
| 7 | 节点上显示黄色小徽章（pending 推荐数） |
| 8 | 三个面板切换按钮可用，面板正确显示/隐藏 |
| 9 | `curl` 测试接口返回正确 JSON |
| 10 | 完整流程：从发现到建联，从面板到规划建议 |

---

**关联设计文档：** [图谱价值增强架构设计](../../superpowers/specs/2026-04-30-图谱价值增强-design.md)
**关联问题清单：** [02-问题清单](../../report/2026-04-29-ui分析/02-问题清单.md) — P2-03, P2-01