# 默认留空：HTTP API 使用同源 /api，后端 WS 使用同源 /api/v1/lakes/{id}/ws。
# 本地 Vite 开发环境由 vite.config.ts 代理到联调后端；生产环境由 nginx 代理 /api。
VITE_API_BASE=
VITE_API_HOST=

# 默认留空：CollabDemo 使用同源 /yjs。
# 若需直连独立 yjs-bridge，可显式设置为 ws://<host>:7790/yjs。
VITE_YJS_BRIDGE_URL=
